# CLAUDE.md

就労継続支援A型事業所（Web制作）の経営シミュレーションゲーム。静的サイト。

## ファイルの役割

| ファイル | 役割 | 変更依頼の行き先 |
|---|---|---|
| `data.js` | 数値・定義のすべて | **バランス調整はここだけ** |
| `sim.js` | ゲームロジック。描画は持たない | ルール自体を変えるとき |
| `world.js` | three.js による3Dオフィス | 見た目・レイアウト |
| `ui.js` | 画面下の操作パネル | 表示項目・タブ |
| `main.js` | ゲームループ、速度、セーブ | 進行の制御 |
| `style.css` | 見た目 | 配色・余白 |
| `build.py` | 1枚のHTMLにまとめる補助 | 基本さわらない |

**「案件の報酬を上げて」「勉強が速すぎる」「大企業の案件が来なさすぎる」といった依頼は、すべて `data.js` の編集で完結する。** 他のファイルを触る前に、まず `data.js` に該当する定数がないか確認すること。

## data.js の主な定数

- `TIME` — 始業・終業・休憩・1ゲーム分の実時間
- `EXP_TO_NEXT` — レベルアップに必要な経験値（Lv1→2 … Lv4→5）
- `MONEY` — 時給、給付費単価、職員給与、固定費
- `STANDARD` — 人員配置基準と減算率
- `PROJECT_TEMPLATES` — 案件の一覧。`effort`（工数）と `pay`（報酬）と `days`（納期）
- `REPUTATION` — 評価に応じた案件の質と量、応募者の量
- `WORK` — 1分あたりの生産量、レベル差の補正
- `STUDY` — 習得速度、講師の効果
- `MORALE` — やる気の増減
- `QUALITY` — 満足度と評価への反映
- `TRANSITION` — 一般就労への移行条件

### 工数の目安

利用者1人が1日に生む作業量は `baseOutputPerMin × 220分` ≒ 88。
`effort` を決めるときは「何人で何日かけさせたいか × 88」で逆算する。

### 報酬の目安

利用者1人1日の賃金は約3,500円、生む作業量は88。つまり**作業量1あたりの人件費は約40円**。
`pay ÷ effort` が 40 を下回る案件は、受けるほど赤字になる。1.5〜2.0倍あたりが標準。

## ゲームの中核となる構造

この2つは崩さないこと。崩すとゲームとして成立しなくなる。

1. **会計は2つに分かれている。** 給付費（通所実績で入る）は職員給与と運営費に充てる。利用者の賃金は生産活動収入（案件の売上）からしか払えない。`fundWelfare` と `fundBusiness` を混ぜない。
2. **勉強と製造は同じ時間を奪い合う。** 勉強モードの利用者は案件を進めない。今日の売上と将来の能力のトレードオフが、このゲームの判断の核。

## 作業ルール

- 変更後は `node --check <file>` で構文を確認する。
- バランスを変えたら、ヘッドレスで数十日回して破綻していないか見る。
- 敗北・廃業は実装しない（設計上の方針）。資金難は警告とイベントで表現する。
- 利用者を数値だけの存在として扱う表現は避ける。ログや文言は敬称をつけ、事務的で誠実な調子を保つ。
- コミットメッセージは日本語で簡潔に（例: `大企業案件の報酬を引き上げ`）。

## 触らないもの

- three.js は CDN の r128 を使用。`CapsuleGeometry` など r142 以降の機能は使えない。
- セーブは localStorage の `atype-save-v1`。データ構造を変えたらキーのバージョンを上げる。
